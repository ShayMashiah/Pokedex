import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";
import {Variant} from "../src/lib/types";

function App() {
  return (
  );
}

export default App;
